[extend]
useDefault = true

[allowlist]
description = "Known false positives"
fingerprints = [
  # NestJS's default scaffolded README ships a placeholder CircleCI badge
  # token in its example URL — not a real credential.
  "8391dd79a3b015a11a12f99898b5d5e4e0e02cc0:apps/api/README.md:generic-api-key:5",
]
